---
language: [ ruby, node_js ]
rvm: [ 2.2.2 ]
node_js: [ iojs ]
cache:
  bundler: true
  directories:
    - bower_components
sudo: false

before_script:
  - npm install -g bower
  - bower install

# Install the travis gem.
# Create a deploy key and encrypt it with:
# $ travis encrypt-file .deploy_key
# Commit the encrypted file and
# replace the first command below with the generated one.
before_install:
  - openssl aes-256-cbc -K $encrypted_02fcf6464ec2_key -iv $encrypted_02fcf6464ec2_iv -in .deploy_key.enc -out .deploy_key -d
  - eval "$(ssh-agent)"
  - 'echo ''gem: --no-document'' > ~/.gemrc'

script: bundle exec rake travis

# Set the branch that will be built and published to GitHub pages.
branches:
  only:
    - source

# Run the command below (fill in your name and email):
# $ travis encrypt 'GIT_NAME="Your Name" GIT_EMAIL=you@example.com'
# Replace the value of secure with your generated value.
env:
  - secure: "GEF6pyGUs1cfzkDYSqQLf7xzOjYqWBYaeV2RU5LgU4ddyBzhd/aizvYvbfKcZFjSxzRMlW/X7bHoNo1jUF28/hYoaELUIHgZjKcLDCl3U8Xb72rldp2UtvFuUH/6nepO4K7ZUJRvp+pDJlnnQ4CVIz50i/sn2m/uQKMP3thj4uY="
